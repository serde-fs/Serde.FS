# Serde.FS

## Local debug build

* Run `dotnet fsi debug-stj.fsx` to build, pack, and test the STJ backend against the SampleApp.
* Run `dotnet fsi debug-stj.fsx -- -p clean` to delete debug packages, caches, and kill build processes.
