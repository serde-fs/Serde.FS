{
  "private": true,
  "scripts": {
    "postinstall": "dotnet tool restore",
    "dev": "dotnet fable watch . -o build --run vite",
    "build": "dotnet fable . -o build --run vite build"
  },
  "dependencies": {
    "lit": "^3.1.3"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
